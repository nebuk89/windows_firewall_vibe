name: RDP via Cloudflare Tunnel

on:
  workflow_dispatch:

jobs:
  rdp-cloudflared:
    runs-on: windows-latest
    timeout-minutes: 360  # 6 hours max
    permissions:
      contents: read
    
    steps:
      - name: Security Notice
        shell: pwsh
        run: |
          Write-Host "⚠️  SECURITY NOTICE" -ForegroundColor Red
          Write-Host "This workflow requires a secret to be configured:" -ForegroundColor Yellow
          Write-Host "  - PASSWORD: RDP password for the runneradmin user" -ForegroundColor Yellow
          Write-Host "" 
          Write-Host "RDP credentials will be displayed in workflow logs." -ForegroundColor Yellow
          Write-Host "Do NOT use this with sensitive data or production systems." -ForegroundColor Yellow
          Write-Host ""
          Write-Host "ℹ️  This workflow uses Cloudflare Tunnel (cloudflared)" -ForegroundColor Cyan
          Write-Host "No Cloudflare account or authentication required!" -ForegroundColor Cyan
          Write-Host ""
      
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download cloudflared
        shell: pwsh
        run: |
          Write-Host "📥 Downloading cloudflared..." -ForegroundColor Cyan
          Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe
          Write-Host "✅ Download complete" -ForegroundColor Green
      
      - name: Enable Remote Desktop
        shell: pwsh
        run: |
          Write-Host "🔧 Enabling Remote Desktop..." -ForegroundColor Cyan
          
          # Enable Remote Desktop
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          
          # Enable RDP through Windows Firewall
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          
          # Enable Network Level Authentication
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
          
          Write-Host "✅ Remote Desktop enabled successfully" -ForegroundColor Green
      
      - name: Configure RDP User
        shell: pwsh
        env:
          RDP_PASSWORD: ${{ secrets.PASSWORD }}
        run: |
          Write-Host "👤 Configuring RDP user..." -ForegroundColor Cyan

          # Validate password is provided
          if (-not $env:RDP_PASSWORD) {
            Write-Host "❌ ERROR: PASSWORD secret is not configured!" -ForegroundColor Red
            Write-Host "" -ForegroundColor Red
            Write-Host "Please configure the PASSWORD secret in your repository:" -ForegroundColor Yellow
            Write-Host "  1. Go to Settings > Secrets and variables > Actions" -ForegroundColor White
            Write-Host "  2. Click 'New repository secret'" -ForegroundColor White
            Write-Host "  3. Name: PASSWORD" -ForegroundColor White
            Write-Host "  4. Value: Your desired RDP password (must meet complexity requirements)" -ForegroundColor White
            Write-Host "" -ForegroundColor Yellow
            exit 1
          }

          # Validate password length (Windows requires at least 6 characters typically)
          if ($env:RDP_PASSWORD.Length -lt 6) {
            Write-Host "❌ ERROR: PASSWORD is too short (minimum 6 characters required)" -ForegroundColor Red
            exit 1
          }

          # Set password for the existing runneradmin user
          try {
            $securePassword = ConvertTo-SecureString -AsPlainText $env:RDP_PASSWORD -Force
            Set-LocalUser -Name "runneradmin" -Password $securePassword -ErrorAction Stop
            Write-Host "✅ User configured successfully" -ForegroundColor Green
          } catch {
            Write-Host "❌ ERROR: Failed to set password: $($_.Exception.Message)" -ForegroundColor Red
            Write-Host "" -ForegroundColor Yellow
            Write-Host "Password may not meet Windows complexity requirements:" -ForegroundColor Yellow
            Write-Host "  - At least 8 characters recommended" -ForegroundColor White
            Write-Host "  - Mix of uppercase, lowercase, numbers, and symbols" -ForegroundColor White
            Write-Host "  - Example: MyP@ssw0rd!" -ForegroundColor White
            Write-Host "" -ForegroundColor Yellow
            exit 1
          }

          Write-Host ""
          Write-Host "Username: runneradmin" -ForegroundColor White
          Write-Host "Password: (configured from secrets)" -ForegroundColor White
      
      - name: Start Cloudflare Tunnel
        shell: pwsh
        run: |
          Write-Host "🚇 Starting Cloudflare tunnel..." -ForegroundColor Cyan
          
          # Create a log file for cloudflared output
          $logFile = "C:\cloudflared.log"
          
          # Start cloudflared as a background job that keeps running
          # Redirect both stdout and stderr to the log file
          $job = Start-Job -ScriptBlock {
            param($exePath, $logFile)
            & $exePath tunnel --url tcp://localhost:3389 2>&1 | Tee-Object -FilePath $logFile
          } -ArgumentList (Resolve-Path ".\cloudflared.exe").Path, $logFile
          
          Write-Host "Cloudflared job started (ID: $($job.Id))" -ForegroundColor Gray
          
          # Wait for tunnel URL to appear in the output (poll the log file)
          Write-Host "Waiting for tunnel to initialize..." -ForegroundColor Yellow
          $tunnelUrl = $null
          $urlPattern = 'https://[a-zA-Z0-9\-]+\.trycloudflare\.com'
          $maxWait = 60
          $waited = 0
          
          while (-not $tunnelUrl -and $waited -lt $maxWait) {
            Start-Sleep -Seconds 2
            $waited += 2
            
            if (Test-Path $logFile) {
              $content = Get-Content $logFile -Raw -ErrorAction SilentlyContinue
              if ($content) {
                $match = [regex]::Match($content, $urlPattern)
                if ($match.Success) {
                  $tunnelUrl = $match.Value
                }
              }
            }
            
            # Also check job output
            $jobOutput = Receive-Job -Job $job -Keep -ErrorAction SilentlyContinue
            if ($jobOutput) {
              $outputText = $jobOutput -join "`n"
              $match = [regex]::Match($outputText, $urlPattern)
              if ($match.Success) {
                $tunnelUrl = $match.Value
              }
            }
            
            Write-Host "." -NoNewline
          }
          Write-Host ""
          
          # Check if job is still running
          $job = Get-Job -Id $job.Id -ErrorAction SilentlyContinue
          if ($job.State -eq 'Failed' -or $job.State -eq 'Completed') {
            Write-Host "❌ Cloudflared exited unexpectedly (State: $($job.State))" -ForegroundColor Red
            Write-Host "Job output:" -ForegroundColor Yellow
            Receive-Job -Job $job
            if (Test-Path $logFile) {
              Write-Host "Log file contents:" -ForegroundColor Yellow
              Get-Content $logFile
            }
            exit 1
          }
          
          if (-not $tunnelUrl) {
            Write-Host "❌ Failed to get tunnel URL after ${maxWait}s" -ForegroundColor Red
            Write-Host "Log file contents:" -ForegroundColor Yellow
            if (Test-Path $logFile) { Get-Content $logFile }
            Write-Host "Job output:" -ForegroundColor Yellow
            Receive-Job -Job $job
            exit 1
          }
          
          # Remove https:// prefix for display
          $cleanUrl = $tunnelUrl -replace "https://", ""
          
          Write-Host ""
          Write-Host "═══════════════════════════════════════════" -ForegroundColor Green
          Write-Host "    🎉 RDP CONNECTION READY!" -ForegroundColor Green
          Write-Host "═══════════════════════════════════════════" -ForegroundColor Green
          Write-Host ""
          Write-Host "🌐 Tunnel URL: $cleanUrl" -ForegroundColor Cyan
          Write-Host "👤 Username: runneradmin" -ForegroundColor Cyan
          Write-Host "🔑 Password: (your PASSWORD secret)" -ForegroundColor Cyan
          Write-Host ""
          Write-Host "═══════════════════════════════════════════" -ForegroundColor Green
          Write-Host ""
          Write-Host "📝 How to connect:" -ForegroundColor Yellow
          Write-Host "  1. Download cloudflared: https://github.com/cloudflare/cloudflared/releases/latest" -ForegroundColor White
          Write-Host "  2. Run on your local machine:" -ForegroundColor White
          Write-Host "     cloudflared access tcp --hostname $cleanUrl --url localhost:13389" -ForegroundColor Cyan
          Write-Host "  3. Open Remote Desktop Connection (mstsc.exe)" -ForegroundColor White
          Write-Host "  4. Connect to: localhost:13389" -ForegroundColor White
          Write-Host "  5. Login with username 'runneradmin' and your password" -ForegroundColor White
          Write-Host ""
          Write-Host "💡 Note: Port 13389 on localhost is just an example, use any available port" -ForegroundColor Yellow
          Write-Host ""
          
          # Verify cloudflared is still running
          $cfProcess = Get-Process -Name "cloudflared" -ErrorAction SilentlyContinue
          if ($cfProcess) {
            Write-Host "✅ Cloudflared process is running (PID: $($cfProcess.Id))" -ForegroundColor Green
          } else {
            Write-Host "⚠️ Warning: Cloudflared process not found - tunnel may not be active" -ForegroundColor Yellow
          }
          
          # Save to environment for job summary
          "TUNNEL_URL=$cleanUrl" | Out-File -FilePath $env:GITHUB_ENV -Append
          "TUNNEL_FULL_URL=$tunnelUrl" | Out-File -FilePath $env:GITHUB_ENV -Append
      
      - name: Generate Job Summary
        shell: pwsh
        run: |
          $summary = @"
          # 🖥️ RDP Connection Ready via Cloudflare Tunnel
          
          ## Connection Information
          
          | Parameter | Value |
          |-----------|-------|
          | **Tunnel URL** | ``$env:TUNNEL_URL`` |
          | **Username** | ``runneradmin`` |
          | **Password** | ``(your PASSWORD secret)`` |
          
          ## 🔌 How to Connect
          
          ### Step 1: Download cloudflared
          Download cloudflared for your operating system:
          - **Windows**: [cloudflared-windows-amd64.exe](https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe)
          - **macOS**: ``brew install cloudflared``
          - **Linux**: [See releases](https://github.com/cloudflare/cloudflared/releases/latest)
          
          ### Step 2: Create local tunnel
          Open a terminal/command prompt and run:
          ``````
          cloudflared access tcp --hostname $env:TUNNEL_URL --url localhost:13389
          ``````
          
          > 💡 You can use any available local port instead of 13389
          
          ### Step 3: Connect via RDP
          1. Open **Remote Desktop Connection** (mstsc.exe on Windows)
          2. Connect to: ``localhost:13389`` (or your chosen port)
          3. Login with:
             - Username: ``runneradmin``
             - Password: ``(your PASSWORD secret)``
          
          ## ✅ Features
          
          - **Cloudflare Tunnel** - Enterprise-grade infrastructure
          - **Better performance** than ngrok (lower latency)
          - **No authentication required** - Uses temporary tunnel
          - **Secure connection** through Cloudflare's network
          
          ## ⏳ Status
          
          The workflow will keep running until:
          - You manually cancel it, or
          - The 6-hour timeout is reached
          
          When you're done, click "Cancel workflow" to stop the job.
          "@
          
          $summary | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8
          
          Write-Host "📝 Job summary generated successfully!" -ForegroundColor Green
      
      - name: Keep Session Alive
        shell: pwsh
        run: |
          Write-Host "⏳ Keeping session alive..." -ForegroundColor Cyan
          Write-Host ""
          Write-Host "The tunnel is now active and will remain open until:" -ForegroundColor Yellow
          Write-Host "  - You cancel this workflow, or" -ForegroundColor Yellow
          Write-Host "  - The 6-hour timeout is reached" -ForegroundColor Yellow
          Write-Host ""
          Write-Host "You can connect anytime using the instructions above." -ForegroundColor Green
          Write-Host ""
          
          # Keep the workflow running and monitor cloudflared
          $elapsed = 0
          $maxTime = 21600  # 6 hours in seconds
          $interval = 60    # Check every minute
          
          while ($elapsed -lt $maxTime) {
            # Check if cloudflared is still running
            $cfProcess = Get-Process -Name "cloudflared" -ErrorAction SilentlyContinue
            if (-not $cfProcess) {
              Write-Host "⚠️ Cloudflared process not found - attempting to restart..." -ForegroundColor Yellow
              
              # Try to restart cloudflared
              Start-Process -FilePath ".\cloudflared.exe" -ArgumentList "tunnel --url tcp://localhost:3389" -WindowStyle Hidden
              Start-Sleep -Seconds 5
              
              $cfProcess = Get-Process -Name "cloudflared" -ErrorAction SilentlyContinue
              if ($cfProcess) {
                Write-Host "✅ Cloudflared restarted (PID: $($cfProcess.Id))" -ForegroundColor Green
                Write-Host "⚠️ Note: Tunnel URL may have changed - check workflow logs" -ForegroundColor Yellow
              } else {
                Write-Host "❌ Failed to restart cloudflared" -ForegroundColor Red
              }
            }
            
            $remaining = $maxTime - $elapsed
            $hours = [math]::Floor($remaining / 3600)
            $minutes = [math]::Floor(($remaining % 3600) / 60)
            
            # Only print status every 5 minutes to reduce noise
            if ($elapsed % 300 -eq 0) {
              Write-Host "⏰ Time remaining: $hours hours $minutes minutes (cloudflared PID: $(if($cfProcess){$cfProcess.Id}else{'N/A'}))" -ForegroundColor Green
            }
            
            Start-Sleep -Seconds $interval
            $elapsed += $interval
          }
          
          Write-Host ""
          Write-Host "✅ Maximum session time reached!" -ForegroundColor Green
